# =============================================================================
# CONFIG: SHARED BUTTON SETTINGS
# =============================================================================
# Shared on/off colors and button display order. Per-button entity, label,
# and icon are defined via button_template.yaml includes in packages.yaml.
# =============================================================================

text:
  - platform: template
    name: "Button On Color"
    internal: true
    id: button_on_color
    optimistic: true
    restore_value: true
    initial_value: "FF8C00"
    min_length: 6
    max_length: 6
    mode: text
    entity_category: config
    icon: "mdi:palette"
    update_interval: never
    web_server:
      sorting_group_id: sg_display

  - platform: template
    name: "Button Off Color"
    internal: true
    id: button_off_color
    optimistic: true
    restore_value: true
    initial_value: "313131"
    min_length: 6
    max_length: 6
    mode: text
    entity_category: config
    icon: "mdi:palette-outline"
    update_interval: never
    web_server:
      sorting_group_id: sg_display

  - platform: template
    name: "Button Order"
    internal: true
    id: button_order
    optimistic: true
    restore_value: true
    initial_value: ""
    min_length: 0
    max_length: 120
    mode: text
    entity_category: config
    icon: "mdi:order-numeric-ascending"
    update_interval: never
    web_server:
      sorting_group_id: sg_button
